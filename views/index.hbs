{{> header}}
<div class="container mb-5 mt-5">
  <h1>Lista de Tasks</h1>
  <table class="table mt-5">
    <thead>
      <tr>
        <th scope="col">ID</th>
        <th scope="col">Tarefas</th>
        <th scope="col">Ações</th>
      </tr>
    </thead>
    <tbody>
      {{#each tasks}}
      <tr>
        <th scope="row">
          <a href="/tasks/{{this.id}}">{{this.id}}</a>
        </th>
        <td>{{this.title}}</td>
        <td>
          <a href="/tasks/{{this.id}}/edit" class="btn btn-primary">Editar</a>
          <form action="/tasks/{{this.id}}?_method=DELETE" method="post">
            <button type="submit" value="submit"
              onclick="return confirm('Deseja realmente excluir a tarefa {{this.title}} ?')" class="btn btn-danger"
              style="display:inline">
              Excluir
            </button>
          </form>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
  <a href="/tasks/create" class="btn btn-success">Adicionar Nova Tarefa!</a>
</div>

<!-- Option 1: Bootstrap Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
  integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>

{{> footer}}